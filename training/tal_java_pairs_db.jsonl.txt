{"text": "NScribe SELECT to Oracle SELECT", "<TAL>SELECT name, balance FROM customer WHERE id = :cust_id INTO :cust_name, :cust_balance;</TAL><JAVA>PreparedStatement stmt = conn.prepareStatement(\"SELECT name, balance FROM customer WHERE id = ?\");\nstmt.setInt(1, cust_id);\nResultSet rs = stmt.executeQuery();\nif (rs.next()) {\n    cust_name = rs.getString(\"name\");\n    cust_balance = rs.getBigDecimal(\"balance\");\n}</JAVA>"}
{"text": "NScribe INSERT to Oracle INSERT", "<TAL>INSERT INTO transaction (id, amount) VALUES (:txn_id, :txn_amount);</TAL><JAVA>PreparedStatement stmt = conn.prepareStatement(\"INSERT INTO transaction (id, amount) VALUES (?, ?)\");\nstmt.setInt(1, txn_id);\nstmt.setBigDecimal(2, txn_amount);\nstmt.executeUpdate();</JAVA>"}
{"text": "NScribe UPDATE to Oracle UPDATE", "<TAL>UPDATE account SET balance = :new_balance WHERE id = :acct_id;</TAL><JAVA>PreparedStatement stmt = conn.prepareStatement(\"UPDATE account SET balance = ? WHERE id = ?\");\nstmt.setBigDecimal(1, new_balance);\nstmt.setInt(2, acct_id);\nstmt.executeUpdate();</JAVA>"}
{"text": "NScribe DELETE to Oracle DELETE", "<TAL>DELETE FROM logs WHERE log_date < :cutoff_date;</TAL><JAVA>PreparedStatement stmt = conn.prepareStatement(\"DELETE FROM logs WHERE log_date < ?\");\nstmt.setDate(1, java.sql.Date.valueOf(cutoff_date));\nstmt.executeUpdate();</JAVA>"}
{"text": "NScribe cursor to Oracle ResultSet", "<TAL>DECLARE cust_cursor CURSOR FOR SELECT id, name FROM customer;</TAL><JAVA>Statement stmt = conn.createStatement();\nResultSet cust_cursor = stmt.executeQuery(\"SELECT id, name FROM customer\");</JAVA>"}
